name: Build Veilbound PR

on:
  pull_request:
    branches: [ main ]

permissions:
  contents: read
  actions: read

jobs:
  validate:
    uses: ./.github/workflows/validate-0.1.60.yml
    secrets: inherit
